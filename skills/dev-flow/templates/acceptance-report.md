# 验收报告：<名称> V<N>

> 由验收 agent 按 dev-flow 流程生成，逐条核对验收清单后填写。
> 验收依据：<验收清单路径>（唯一依据）；不看实现代码、不读需求描述。

## 基本信息

| 字段 | 值 |
|---|---|
| 需求文档 | <路径> |
| 验收清单 | <路径> |
| 验收执行 | 验收 agent（fresh context） |
| 验收日期 | YYYY-MM-DD |
| 总体结论 | 通过 / 不通过 / 部分通过（待人工确认） |

## 验收结果（逐条）

| 编号 | 验收项 | 验证层级 | 状态 | 证据 | 失败原因 |
|------|--------|----------|------|------|----------|
| 1.1 | （来自验收清单） | [单测] | 通过 | 命令 + 输出摘要 | |
| 1.2 | …… | [E2E-人工] | 待人工确认 | 操作说明 | |

状态取值：**通过 / 失败 / 未执行 / 待人工确认**
- 未执行 = 不通过（不得以任何理由跳过）
- 证据必须可复现：命令输出、截图、操作记录；禁止"人工验证"一词带过

## 补充测试用例（验收 agent 独立设计）

| 测试用例 | 针对验收项 | 覆盖场景 | 结果 | 证据 |
|----------|-----------|----------|------|------|
| …… | 1.x | 异常/边界（编码 agent 未覆盖） | 通过/失败 | 命令输出 |

> 由验收 agent 从验收清单独立推导（不修改已有测试，只新增独立文件）；结果并入验收证据。

## 失败原因摘要（有失败时必填）

| 编号 | 失败原因 | 初步分类（编码 bug / 需求缺陷 / 环境数据） |
|------|----------|---------------------------------------------|
| 1.x | …… | …… |

主 agent 依据本表做失败分类，决定回编码 / 回 prd-md / 处理环境。

## 人工确认项（[E2E-人工] 类）

| 编号 | 验收项 | 操作说明（怎么验） | 状态 |
|------|--------|--------------------|------|
| 1.x | …… | 步骤：打开页面 → 操作 → 观察 | 待用户确认 |

验收 agent 不自行声称人工项通过；此项由用户按操作说明验证后确认。

## 结论

- 全部通过（人工项已确认）→ 可进入完成，归档本报告
- 有失败 → 主 agent 按失败原因摘要分类并安排下一轮
